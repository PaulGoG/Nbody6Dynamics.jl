---
name: Bug report
about: Something the package does wrong
labels: bug
---

**What I ran**

The configuration file (or the merger TOML) and the command.

**What happened**

The error or the wrong result, with the relevant part of
`runs/<id>/nbody6dynamics.log` or of the Julia stack trace.

**What I expected**

**Environment**

- Julia version:
- Package version or commit:
- Engine commit (`install.ref`, or `backend_commit` in `RUN_INFO.toml`):
- The `[hardware]` table of `RUN_INFO.toml`, when a run is involved:

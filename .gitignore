# Backend (cloned N-body source + build artefacts)
backend/

# Run outputs (simulation data, plots, animations)
runs/

# Root-level plot output (default output_dir when no run directory is given)
plots/

*.h5part
*.hdf5

# Documenter.jl build output
docs/build/

# Benchmark runs and results
bench/runs/
bench/results/

# Literate output of the documentation walkthrough (generated by docs/make.jl)
docs/src/walkthrough.md

# Resolved environments: reproducibility rests on Project.toml + [compat];
# each run keeps its own manifest snapshot (environment_manifest.toml)
Manifest.toml
Manifest-v*.toml
